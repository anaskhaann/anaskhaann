# Copy to .env and fill in, then: uv run today.py  (today.py auto-loads .env)
# Fine-grained PAT — all repositories, read: Contents, Commit statuses,
# Issues, Metadata, Pull Requests
ACCESS_TOKEN=YOUR_TOKEN
USER_NAME=YOUR_USERNAME
